{
  "buildCommand": "npm exec cross-env SENTRY_REQUIRE_CONFIG=true node scripts/with-app-version.mjs npm run build --workspace @mailmind/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
